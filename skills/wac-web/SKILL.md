---
name: wac-web
description: Open the Hermes dashboard for keys, models, credits. WAC web /dash /ui. Not /web.
---
# WAC web

Trigger: `WAC web`, `/dash`, `/ui`, or "open the dashboard."

`/web` is a different skill (web-navigator) — official vendor pages.
Do not steal that meaning.

Tell them to double-click **Web Interface** on the stick, or run:

    <DRIVE>:\north-forge-agent-venv\Scripts\hermes.exe dashboard

with HERMES_HOME on that same drive's `north-forge-agent-data`.

Use for engine change, 30-day key paste, credit check, free-model fallback.
Do not configure PATH. Do not type bare hermes.
