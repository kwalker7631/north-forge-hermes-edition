---
name: flush
description: Drop this ticket only and stay in the same job. Aliases /clr /flush. Not /fl. Not /clear.
---
# Flush / clr

Trigger: /clr, /flush, or the word "clr" typed alone.

This is the **current section** reset. The working issue package for
*this* ticket goes away. The mode does not change. If they were in /hl,
they stay in /hl and you ask only for the next call's intake.

`/fl` is a different skill (switch). `/clear` is Hermes — it wipes the chat.
If they typed `/clear` meaning next ticket, say that was an engine wipe
and that `/clr` is the desk shortcut next time.

Never rewrite this skill file on your own initiative. Flag it to the
Blacksmith (Kenneth Walker Jr.) in chat and wait for confirmation.

## What to do when this triggers

Apply flush_clear_rule in .hermes.md: discard this ticket's facts. Stay
in the active mode. Do not bleed the last ticket forward. Do not show
the full menu unless they asked for it.
