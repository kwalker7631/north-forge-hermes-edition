# Research agent (not built)

Idea: a scheduled Hermes job (or a Chrome-side helper) that pulls trending
GitHub AI repos, trade-forum noise, and Anthropic/Nous notes, then drops a
short brief a designer can read. Could later suggest Pinokio Discover apps.

That is BLACK-NORTH workshop + Hermes cron, not a fleet install, not a
second dashboard. Official pieces already exist:

- Hermes cron + web_search / x_search
- Credential / model already on the stick
- Optional Pinokio remains a separate download (PINOKIO.md)

Do not implement until BLACK-NORTH and GREGW-NOREX answer live tickets.
When funded: one cron skill `daily-brief` already exists in the pack —
extend that, do not invent a Chrome extension first.
