# Before you format anything

Git is current on the private pack (`main`). The USB is not, until you
pull or rebuild. Formatting does not update Git. Git does not format.

## Not broken (leave it)

- Deploy Console path: `Advanced\deploy-console\` (not moved to etc)
- RAW disk + browse box
- Set-Inference.ps1 and Check-HermesPath.ps1
- Stick venv + HERMES_HOME isolation
- `/clr` vs `/fl` vs Hermes `/clear`
- Hermes wordmark + gold cooperation caption
- Theme `north-forge-kyocera`

## Gaps that are paper, not wipe-blockers

- Humanizer was doctrine in SOUL.md only — now also `skills/humanizer/`
- `/web` still means official vendor pages. Dashboard is `WAC web` / `/dash`
- Skill allowlists are files, not yet wired into Zero-Touch
- Dashboard key save not watched in a browser from this chat
- Caption slot not seen in a live browser
- Pinokio not on the stick until BLACK-NORTH answers
- Older docs (EXCALIBUR.md EXCAL-NOR01, logs with D:) are history

## Do this, then wipe

1. `gh auth login` still good on the admin PC.
2. Pull engine + pack (HAND_HOLD_PULL.md). Nested path only:
   `X:\north-forge-agent\private-editions\kyocera`
3. Confirm `git -C ...\kyocera log -1 --oneline` shows a commit from
   tonight (BEFORE_YOU_WIPE or later).
4. Format BLACK-NORTH (exFAT teammate-size, or NTFS if Pinokio this week).
5. Label `BLACK-NORTH`. Full tier. Passcode. Deploy.
6. Set-Inference or dashboard Env — Anthropic, corp or 30-day burner.
7. Start North Forge. One real code. Then Greg as `GREGW-NOREX`.

If pull shows local edits, stop. Do not reset --hard.
