# Phone as a window (later)

The stick on USB-C:

- Android with OTG: files visible. Hermes CLI is still Windows/macOS/Linux.
- iPhone USB-C + adapter: same story — storage, not the engine.

Useful path later: Hermes gateway (Telegram / a small relay) so a tech speaks to the agent from the phone while the stick or a knight PC runs the engine. Hands-free `/voice` stays on the PC session.

Not this wipe.
