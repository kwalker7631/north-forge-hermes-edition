# Handoff for Claude — North Forge backend

**When:** 12 September 2026, 21:52 EDT  
**From:** Grok session with Kenneth C. Walker Jr.  
**For:** Claude, owning backend / engine wiring  
**Repos:** `kwalker7631/north-forge-agent` (public), `kwalker7631/north-forge-hermes-edition` (private)

## Mission

Portable TSC go-to on a USB. One engine. No second GPT. Public = chassis.
Private = Kyocera pack. Hermes is the runtime, thanked last.

## Desk contract

Combined: `/hl /chk 4054ci: Failure to push terminal MyQ to machine` — look first, then write the ticket in one answer.

Resets: `/clr` this ticket stay in mode. `/fl` whole NF skill session (our clear). Hermes `/clear` is the chat wipe.

Aliases: `ALIASES.md`. Soul: `SOUL.md` must assemble into `.hermes.md`.

## Build next

1. `/model` `/models` `/provider` — list configured Gemini / Claude / Grok (and whatever `hermes model` already has). One-key pick. Persist on the drive. No `.env` editing for techs. Do not steal `/web`.
2. `/webui` `/ui` — Phase A: print/open local gateway URL. No public tunnel. Sidebar is Phase B.
3. Combined-slash router test. `/check` look-up order on forge-audit. SCOPE-05 retired launcher tests.

Do not restyle READMEs. Do not put Pinokio on the 32 GB Excalibur stick.

Paste: You own North Forge backend. Read this file. Combined /hl /chk, /clr vs /fl, then /model and /webui Phase A.
